import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Interactive Prompt Playground",
  description: "Test different prompt configurations and parameters to see how they affect AI-generated product descriptions. Compare outputs across various temperature, token limits, and penalty settings.",
  keywords: ["AI", "OpenAI", "GPT", "Prompt Engineering", "Product Descriptions", "Machine Learning"],
  authors: [{ name: "Interactive Prompt Playground" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
